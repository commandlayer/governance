# AGENTS Template: runtime-core

## Repository Role

Own foundational runtime primitives and core execution contracts.

## Instructions

1. Core behavior MUST trace to protocol definitions.
2. Contract changes MUST document downstream runtime impact.
3. Changes with compatibility risk MUST update matrix evidence inputs.
4. Runtime-core MUST NOT redefine protocol authority.
